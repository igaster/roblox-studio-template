# Built place files
*.rbxl
*.rbxlx

# Roblox Studio lock files
/*.rbxlx.lock
/*.rbxl.lock

# Rojo sourcemap (generated, not source)
sourcemap.json

# Sandbox: local git clones used to dogfood the framework (orchestrator/subagent
# game-building exercises). Never part of the template itself.
/sandbox/

# Orchestrator-only skill for the dogfooding process above — meta-tooling for
# developing this framework, not something a real template clone should inherit.
/.claude/skills/framework-dogfooding/

# Dogfooding-only subagent personas — same reasoning as framework-dogfooding above.
/.claude/agents/critic.md
